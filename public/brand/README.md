# SomEducation brand assets

SVG logo sources for the in-app wordmark (`src/components/brand/som-education-logo.tsx`).

The live logo is rendered as an inline SVG component — no separate upload step is required for auth or the marketing site.
